using Common.Scripts;
using UnityEngine;

public class StaffMotor : Motor
{
    public StaffMotor(Rigidbody rb, Transform modelTransform, Animator animator, float maxSpeed) : base(rb, modelTransform, animator, maxSpeed)
    {
        
    }
}
